# WordFlow 产品设计方案

> 基于《免费英文词典学习应用-深度研究报告》
> 版本：v1.1 ｜ 日期：2026-07-27
> 产品代号：**WordFlow**（暂定，寓意"查词到记忆的顺畅流动"）
> v1.1 变更：技术架构、数据管线、AI 能力、PWA、质量保障全面对齐 devpath-ai（commit 23a40798）

---

## 1. 产品定位

### 1.1 一句话定位

**"查词即背词"的免费英语词典学习工具** —— 把欧路的查词、墨墨的算法、Anki 的自由度合成一个不要钱、不折腾、无广告的产品。

### 1.2 定位三角

| 维度 | 定位 | 对标反面 |
|------|------|---------|
| 价格 | 核心功能 100% 免费，永不残血 | 墨墨的单词上限、扇贝的付费墙 |
| 体验 | 无广告、无信息流、零配置开箱即用 | 有道的臃肿、欧路/Anki 的折腾 |
| 数据 | 本地优先、离线可用、随时导出 | 所有竞品的"数据锁死" |

### 1.3 目标用户

- **主力画像 A「备考的小林」**：23 岁，考研二战，每天背 150 词。痛点：墨墨免费上限不够用，查词还得切有道，生词本两边不同步。
- **种子画像 B「读文档的阿哲」**：27 岁，后端工程师，每天读英文技术文档。痛点：欧路划词要付费，查过的词转头就忘，从不复习。
- 共同特征：18-30 岁、自驱学习、对广告和付费墙敏感、手机+电脑双端使用。

### 1.4 非目标（明确不做）

❌ 社区/信息流/商城 ｜ ❌ K12 教材同步 ｜ ❌ 多语种 ｜ ❌ 强制注册登录 ｜ ❌ 直播课/卖课

---

## 2. 核心产品逻辑

### 2.1 核心闭环

```
遇到生词 → 极速查词 → 一键收藏（自动入 FSRS 队列）→ 每日复习 → 递进验证 → 记忆固化
```

**关键设计决策：查词与背词共享同一份卡片数据。** 查词页的"收藏"按钮不是加入"生词本"（名词），而是加入"复习队列"（动词）——收藏那一刻 FSRS 卡片即创建，次日自动出现在复习队列，无需任何额外操作。这是与所有竞品的根本差异。

### 2.2 记忆验证四级状态机（复用 devpath-ai V1-V4 思想）

| 级别 | 验证方式 | 回答的问题 | 触发时机 |
|------|---------|-----------|---------|
| V1 再认 | 看词选义 / 看义想词 | 见到认识吗 | 新卡前 3 次复习 |
| V2 拼写 | 听音拼写 / 首字母填空 | 写得出来吗 | Stability > 7 天后解锁 |
| V3 语境 | 真实例句挖空选择 | 语境里会用吗 | Stability > 21 天后解锁 |
| V4 产出 | AI 造句批改（可选） | 能主动使用吗 | 用户手动触发 |

设计原则：**级别越高出现频率越低**，避免复习负担爆炸。V2-V3 答错会将卡片降级并反馈给 FSRS（评为 Again/Hard）。

### 2.3 词书与自由收藏双轨制

- **词书轨**：选考试词书（考研/四六级/雅思/托福/COCA 高频），按每日新词数推进，满足"系统备考"。
- **收藏轨**：查词随手收藏的词直接入队，满足"随遇随记"。
- 两轨汇入**同一个 FSRS 复习队列**，去重合并（同一单词只有一张卡，来源打标签）。

### 2.4 产品四层架构（对齐 devpath-ai L1-L4 心智模型）

把 WordFlow 现有功能映射到 devpath-ai 的四层心智模型，明确每层职责。**注意：不照搬 devpath-ai 域特有的能量回归/情绪觉察/作品集模块**（开发者成长 OS 域），而是保留 WordFlow"克制"定位，把对应层用词典学习的等价能力填充。

```
┌─────────────────────────────────────────────────────────────────┐
│  L4 交付层（解决"坚持"和"记忆"）                                    │
│  FSRS 复习调度 · Streak + 热力图 · PWA Service Worker                │
│  （stale-while-revalidate + Web Push + periodicsync 后台呼吸）       │
├─────────────────────────────────────────────────────────────────┤
│  L3 验证层（解决"记住了吗"的客观验证）                                │
│  V1 再认（看词选义）→ V2 拼写（听音拼写）→                          │
│  V3 语境（例句挖空）→ V4 产出（AI 造句批改）                         │
│  纯函数 applyVerificationResult 推进，不可跳级                       │
├─────────────────────────────────────────────────────────────────┤
│  L2 路径层（解决"先学什么后学什么"）                                  │
│  词书轨（系统备考：考研/CET/雅思/托福/COCA）+                         │
│  收藏轨（随遇随记）→ FSRS 单队列去重合并                              │
├─────────────────────────────────────────────────────────────────┤
│  L1 内容层（解决"学什么"和"内容权威性"）                              │
│  ECDICT（77 万词条 MIT）+ WordNet + Wiktionary + COCA/BNC 词频       │
│  T0-T3 来源等级 + Content-as-Code 编译 + zod 校验 + G1-G7 图谱校验   │
└─────────────────────────────────────────────────────────────────┘
```

**四个矛盾化解对应表**（适配词典学习域）：

| 矛盾 | WordFlow 的解法 | 对应架构层 |
|------|----------------|-----------|
| 通用词典无方向，查完就忘 | 词书轨 + COCA 高频排序 + 收藏即入队 | L1 内容层 + L2 路径层 |
| 学了就忘，不知道记没记住 | V1 再认 → V2 拼写 → V3 语境 → V4 产出 | L3 验证层 |
| 坚持不下去，复习中断 | FSRS 调度 + Streak + PWA periodicsync 主动召回 | L4 交付层 |
| 查词与背词割裂（竞品通病） | 收藏即创建 FSRS 卡片，次日自动入队 | L2 + L4 跨层闭环 |

---

## 3. 功能设计

### 3.1 功能架构

```
WordFlow
├── 查词（Search）
│   ├── 极速搜索：前缀补全 + 模糊纠错，首屏 <100ms
│   ├── 词条页：音标/发音/释义/词频星级/词根词缀/例句/同近义词
│   ├── 语义反查：描述意思找单词（P1，向量检索）
│   └── 一键收藏入队
├── 学习（Learn）
│   ├── 今日队列：新词学习 + 到期复习，合并展示
│   ├── 复习卡片：认识/模糊/忘记 三档反馈 → FSRS 调度
│   ├── 训练模式：拼写/听写/语境填空（V2/V3）
│   └── AI 助记与个性化例句（P1，限流+BYOK）
├── 词书（Books）
│   ├── 预置词书：考研/四六级/雅思/托福/COCA
│   ├── 自定义词书：粘贴单词列表一键生成
│   └── 进度管理：每日新词数、剩余天数预估
├── 统计（Stats）
│   ├── Streak 连续打卡 + 学习热力图
│   ├── 词汇量估算与记忆保持率曲线
│   └── 学习报告分享卡片（图片+二维码）
└── 我的（Me）
    ├── 云同步（手动触发，匿名 ID）
    ├── 数据导出：JSON / CSV / Anki apkg
    ├── FSRS 参数：目标保持率 0.8/0.9/0.95 三档（aggressive/standard/conservative）
    ├── AI 设置：Provider 切换（GLM/DeepSeek/MiMo/custom）+ Trial 剩余额度 + BYOK
    └── AI 连接测试：/api/ai-test 一键探测 BYOK Key 是否有效
```

### 3.2 关键页面设计

#### A. 查词页（首页，产品门面）

- 打开即聚焦搜索框（像 Google 一样克制），下方仅两行：今日待复习数量提醒 + Streak 状态。
- 输入即搜（debounce 80ms），候选列表带词频星级和"已收藏"标记。
- 词条页信息层级：**音标+发音按钮 → 核心释义（按词频排序）→ 词频/考纲标签 → 词根词缀助记 → 例句（≤3 条，可换）→ 同近义词**。
- 右上角固定"＋收藏"大按钮；收藏后变为"已在队列，明天复习"，点击可设置优先级。
- 底部（P1）：「AI 例句」按钮 —— 按用户身份生成语境例句（程序员看到 `deploy` 的 DevOps 语境）。

#### B. 复习页（留存核心）

- 全屏卡片流，正面：单词 + 音标 + 自动发音；点击/空格翻面显示释义与例句。
- 三按钮：`忘记`（红）/`模糊`（黄）/`认识`（绿），映射 FSRS 的 Again/Hard/Good，长按`认识`= Easy。
- 顶部进度条显示今日剩余；完成后展示结算页：今日数据 + Streak+1 动画 + 明日预告。
- 键盘快捷键（1/2/3/空格）—— 桌面端体验是对程序员群体的尊重。
- **中断友好**：随时退出，进度实时落库（本地优先的天然优势）。
- **PWA periodicsync 主动召回**：用户授权通知权限后，浏览器后台（即使应用未打开）按周期检查 due 卡片，推送"📚 今日有 N 词待复习"通知；点击通知直达本页 `/review`。详见 4.5.3。

#### C. 词书页

- 卡片式陈列，显示总词数/已学/预计完成日。
- 选书后引导设置每日新词数（默认 20，滑杆实时预估完成日期）。
- 自定义词书：文本框粘贴单词（每行一个或逗号分隔）→ 自动匹配词典 → 生成词书。

#### D. 统计页

- GitHub 风格年度热力图（react-activity-calendar 直接复用）。
- 记忆保持率曲线（FSRS 可直接计算预期保持率，这是墨墨没有的透明度）。
- 「分享战绩」：html-to-image 生成海报 + 二维码（低成本增长裂变）。

### 3.3 交互原则

1. **三秒完成一次查词收藏**：搜索 → 点结果 → 点收藏，全程 ≤3 步。
2. **零强制**：不强制注册、不强制打卡、不强制看广告；错过复习不焦虑轰炸（FSRS 天然容忍拖延，逾期卡片自动重排）。
3. **默认静音的游戏化**：Streak 和热力图提供正反馈，但无排行榜、无 PK、无虚拟货币。

### 3.4 PWA 交互（后台呼吸）

- **通知直达**：periodicsync 推送的通知点击后直达 `/review`，省去用户打开应用→导航的两步。
- **通知文案分级**（基于最后学习时间）：
  - 当日有待复习卡片 → "📚 今日有 N 词待复习"
  - 连续 3 天未学习 → "回来背词吧，复习队列在等你"
  - 连续 7 天以上未学习 → "很久没背词了，词书在等你"（换文案避免麻木）
- **通知静默开关**：用户可在"我的"里关闭 periodicsync 通知，仅保留应用内提醒——尊重"零强制"原则。
- **渐进增强**：不支持 periodicsync 的浏览器降级为应用打开时的首屏提醒（不丢失核心闭环）。

---

## 4. 技术架构（对齐 devpath-ai）

### 4.1 总体架构（L1-L4 四层 + 三运行时双视图）

```
┌─────────────────────────────────────────────────────────────────┐
│  L4 交付层 ── FSRS 复习 · Streak · PWA Service Worker            │
│  L3 验证层 ── V1 再认 → V2 拼写 → V3 语境 → V4 产出              │
│  L2 路径层 ── 词书轨 + 收藏轨 → FSRS 单队列去重                  │
│  L1 内容层 ── ECDICT + WordNet + Wiktionary + COCA/BNC           │
├─────────────────────────────────────────────────────────────────┤
│  运行时 1：浏览器（Next.js 15 App Router + React 19 RSC）          │
│  ├── 查词引擎：本地切片索引 + 前缀树 + 模糊纠错                    │
│  ├── FSRS 引擎：ts-fsrs 4.5（纯本地计算，3 预设 0.95/0.9/0.8）     │
│  ├── 存储层：Dexie.js / IndexedDB（本地优先，dueAt 索引）          │
│  └── Service Worker：stale-while-revalidate + Web Push + periodicsync │
├─────────────────────────────────────────────────────────────────┤
│  运行时 2：Cloudflare Pages Functions（edge runtime）              │
│  ├── /api/auth/exchange  零信任 Session 签发（AES-GCM + 审计）     │
│  ├── /api/sync           KV 增量同步（LWW + tombstone 清理）       │
│  ├── /api/ai/*           AI 代理（sessionSecret + nonce + HMAC）   │
│  ├── /api/ai-test        BYOK 连接测试（区分上游 401 vs 本地 500）  │
│  └── /api/embed          Workers AI bge 文本嵌入                  │
├─────────────────────────────────────────────────────────────────┤
│  运行时 3：数据层（构建期编译 + CDN + KV + Workers AI）            │
│  ├── 静态：dict/{a-z}/{prefix}.json · books/{id}.json · semantic-index.json │
│  ├── KV namespaces：KV（用户数据）/ AUTH_SESSIONS / AUTH_NONCES / AUTH_AUDIT │
│  └── Workers AI：bge-base-en-v1.5 嵌入（免费 10K 次/日）           │
└─────────────────────────────────────────────────────────────────┘
```

**与 v1.0 差异**：从"前端/静态/Edge"三层平铺升级为"L1-L4 产品层 + 三运行时"双视图；补齐 PWA Service Worker、三 KV 隔离、`/api/ai-test`、`/api/auth/exchange` 独立端点。

### 4.2 数据管线（Content-as-Code 全流程）

| 脚本 | 输入 | 输出 | 对应 devpath 脚本 |
|------|------|------|------------------|
| `compile-dict.ts` | ECDICT CSV + COCA/BNC 词频 + 词根库 | 按前缀切片的 `dict/*.json`（单片 <50KB） | compile-content.ts |
| `compile-books.ts` | 考纲词表 YAML（zod 校验） | `books/{id}.json` | export-presets.ts |
| `build-semantic-index.ts` | 高频 2 万词释义 BGE 预嵌入 | `semantic-index.json` | build-knowledge-index.ts |
| `audit-dict.ts` | 词典数据 | CI 校验：G1-G7 图谱规则 + 缺释义/缺音标/死链 | audit-source-freshness.ts |

词典数据源（全开源，规避版权）：**ECDICT**（77 万词条 MIT）+ **WordNet**（同近义词）+ **Wiktionary**（词源）+ COCA/BNC 词频表。

#### 4.2.1 T0-T3 来源等级体系（对齐 devpath-ai 权威体系）

| 等级 | 含义 | 词典域示例 |
|------|------|-----------|
| T0 | 一手规范 | ECDICT（MIT 协议）、WordNet、Wiktionary 词源 |
| T1 | 一手实现 | COCA/BNC 原始语料词频表 |
| T2 | 权威工程实践 | 教研机构公开考纲词表（考研/CET/雅思/托福官方词表） |
| T3 | 二手解读 | 第三方整理词根库，仅作补充 |

**规则**：每词条 ≥1 条 T0/T1 来源；词书节点 ≥2 条来源。

#### 4.2.2 zod schema 校验 + G1-G7 图谱校验（CI 强制）

词书 YAML 用 **zod schema** 校验 id 格式 / 日期 / 必填字段。CI 跑 **G1-G7 图谱校验**（适配词典域）：

| 规则 | 校验内容 |
|------|---------|
| G1 | 词书引用的词条在词典切片中存在（无悬空引用） |
| G2 | 释义完整性（核心释义非空、音标非空） |
| G3 | 词频标签与 COCA 数据一致 |
| G4 | 同近义词反向链接闭环（A 的同义词列表含 B → B 的列表含 A） |
| G5 | 词书无重复词条 |
| G6 | 自定义词书导入匹配率 ≥ 阈值（默认 80%） |
| G7 | 语义反查向量索引覆盖率（高频 2 万词 100% 覆盖） |

#### 4.2.3 新鲜度巡检

`audit-source-freshness.ts` 检查词典数据源版本与上游同步状态，CI `content:freshness` 步骤强制执行，过期源阻塞合入。

### 4.3 本地数据模型（Dexie 深化，对齐 devpath-ai 索引优化）

```
Dexie schema 版本演进：
v1: kv: "&key, prefix, updatedAt"
v2: kv: "&key, prefix, updatedAt, dueAt"   ← 新增 dueAt 索引

KVRecord {
  key: string;        // 主键：完整 key（如 "card:abandon"）
  value: unknown;     // 原始 value（Dexie 透传）
  prefix: string;     // 前缀（如 "card:"），用于按前缀枚举
  updatedAt?: string; // 从 value.updatedAt 提取，用于增量同步
  dueAt?: string;     // 从 value.due 提取，仅 card 有，用于 countDueCards 精准查询
}

key 前缀规划：
card:{word}      FSRS 卡片（due/stability/difficulty/state/reps/lapses + 来源标签 + V 级别）
log:{date}       每日学习日志（新词数/复习数/正确率）
book:{id}        词书进度（cursor/dailyNew/startedAt）
streak           连续打卡状态
settings         FSRS 参数/主题/AI 配置（MODEL_CONFIG 不参与同步）
tombstone:{key}  删除墓碑（TTL 30 天）
```

**性能优化**（对齐 devpath-ai dueAt 索引）：`countDueCards(now)` 走 dueAt 索引查询（O(due)），避免全量加载卡片（O(n)）。500 张卡片场景下从全量扫描降到仅查到期卡片。

**SSR/Edge 安全**：动态 `import("dexie")`，避免 BroadcastChannel 泄漏到 edge runtime（edge runtime 不支持 BroadcastChannel，静态 import 会让整个路由编译失败）。

**idb-keyval → Dexie 幂等迁移**：以 key 为主键覆盖写入，`_dexie_migrated` 标志位防止重复迁移；不删除旧数据作为备份。

**增量同步 API**（对齐 devpath-ai `lib/storage/db.ts`）：
- `getChangesSince(ts)`：按 updatedAt 索引范围查询，拉取增量变更（含 tombstone）
- `bulkPutItems(items)`：批量写入（同步合并用）
- `listItemsByPrefix(prefix, limit)`：按前缀枚举（如列出所有 `card:`）
- `countByPrefix(prefix)`：按前缀计数

**Tombstone 清理**：`cleanExpiredTombstones()` 定期清理 30 天 TTL 墓碑，避免 KV 无限膨胀。

**同步安全红线**：`settings.MODEL_CONFIG`（含明文 apiKey）**不参与同步**，仅本地存储，换设备需重新输入——避免 userId 泄漏即所有 AI Key 泄漏。

同步策略：`card:`/`log:`/`book:` 前缀参与 KV 增量同步，LWW（Last-Write-Wins）按 `value.updatedAt` 较新者为准；`settings` 敏感项仅本地。

### 4.4 AI 能力接入（零信任 Session 全套细节）

#### 4.4.1 AI 能力矩阵（Provider 从单一 DeepSeek 扩为四家）

| 场景 | Trial 默认 | 备选 | BYOK | 免费额度（Trial/IP） |
|------|-----------|------|------|---------------------|
| 个性化例句 | GLM | DeepSeek / MiMo | custom 不限 | 20 次/日 |
| 词根助记生成 | GLM | DeepSeek / MiMo | custom 不限 | 10 次/日 |
| 造句批改（V4） | GLM | DeepSeek / MiMo | custom 不限 | 5 次/日 |
| 语义反查嵌入 | Workers AI bge-base-en-v1.5 | — | — | 10K 次/日（免费层） |

**Provider 抽象**（对齐 devpath-ai `lib/ai/provider.ts`）：通过 Vercel AI SDK `createOpenAI()` 统一封装 GLM / DeepSeek / MiMo / custom 四家（均兼容 OpenAI 格式），`getModelFromSession(session, scene)` 从 session 上下文取已解密 apiKey 调用。

| Provider | baseURL | 默认模型 | 场景 |
|----------|---------|---------|------|
| glm | `https://open.bigmodel.cn/api/paas/v4` | `glm-4-flash` | Trial 默认，国内零梯子可达 |
| deepseek | `https://api.deepseek.com/v1` | `deepseek-chat` | 备选 Trial |
| mimo | `https://api.xiaomimimo.com/v1` | `mimo-v2-pro` | 备选 Trial |
| custom | 用户自定义 | 用户自定义 | BYOK |

#### 4.4.2 三 KV Namespace 隔离（对齐 devpath-ai 配额隔离设计）

| Binding | 用途 | 设计理由 |
|---------|------|---------|
| `KV` | 用户数据备份 + 公开主页 | 业务数据 |
| `AUTH_SESSIONS` | Session 密文存储（AES-GCM 加密的 apiKey + sessionSecret） | 隔离配额争用 |
| `AUTH_NONCES` | 防重放 nonce（TTL 5min，一次性消费） | 高频写入需独立配额 |
| `AUTH_AUDIT` | 审计日志（exchange/revoke 等敏感操作） | 合规隔离 |

Cloudflare KV 免费档每 namespace 独立 100K 读/日、1K 写/日，四 namespace 隔离避免 session 高频写打爆业务数据 KV。

#### 4.4.3 零信任 Session Exchange 流程（对齐 devpath-ai `app/api/auth/exchange/route.ts`）

```
客户端                              服务端 (/api/auth/exchange)
  │                                    │
  │── POST {apiKey, userId, provider,  │
  │         baseURL, model, name}  ──→ │ 1. 检查 MASTER_KEY（未配置返回 500，不泄露细节）
  │                                    │ 2. 生成 sessionId（16B 随机 → 32 hex）
  │                                    │ 3. 生成 sessionSecret（32B 随机 → base64）
  │                                    │ 4. AES-GCM-256 加密 apiKey 和 sessionSecret
  │                                    │    （12B 随机 IV + 128bit tag，输出 base64(IV+cipher+tag)）
  │                                    │ 5. 构造 SessionRecord 写入 AUTH_SESSIONS KV
  │                                    │ 6. 审计日志写入 AUTH_AUDIT
  │                                    │    （userIdHash=SHA-256(userId) + IP + UA，明文不入日志）
  │←──── {sessionId, sessionSecret,    │ 7. 返回（sessionSecret 仅此次响应中出现）
  │        expiresAt}                  │
```

**SessionRecord 结构**：

```typescript
{
  userId: string;
  encryptedApiKey: string;   // AES-GCM 密文
  encryptedSecret: string;   // AES-GCM 密文
  provider: string;
  baseURL: string;
  model: string;
  name: string;
  createdAt: string;         // ISO
  lastUsedAt: string;        // ISO
  expiresAt: string;         // ISO
}
```

#### 4.4.4 Session 安全四件套（对齐 devpath-ai `lib/ai/session-middleware.ts` + `lib/ai/crypto.ts`）

- **防重放**：每请求带 `timestamp + nonce`；nonce 一次性消费，TTL 5min，存独立 `AUTH_NONCES` KV。
- **防篡改**：`HMAC-SHA256(sessionSecret, canonicalRequest)` 常数时间比对（`constantTimeEqual`，防 timing attack）。
- **滑动续期**：每次成功调用刷新 `expiresAt = now + 7d`，活跃用户不掉线，非活跃用户 7 天后自动过期。
- **Web Crypto API only**：仅用 `crypto.subtle`（edge runtime 原生支持），禁止 Node.js 专属 API（`crypto` 模块）。

#### 4.4.5 Prompt Registry（对齐 devpath-ai `lib/ai/prompts.ts`，v1.0 完全缺失）

所有 AI system prompt 集中管理，每个 prompt 带 `id / version / system / changelog`：

- `promptFingerprint(promptId, version)` 计算版本指纹（如 `"word_sentence:v1:5e8b3a01"`），写入 `AICallRecord.promptVersion`，AI 调用全链路可追溯。
- 修改 prompt 必须 **bump version + 写 changelog**；快照测试 `__tests__/prompts.test.ts` 的 `PROMPT_VERSION_HASHES` 强制拦截"改内容不改 version"。
- **Content Quality Charter**：所有"产答案"的 prompt（AI 例句 / 词根助记 / 造句批改）注入统一质量约束——语境真实 / 无歧义 / 词频适配用户当前水平 / 不含敏感词 / 不编造不存在的词义。

#### 4.4.6 `/api/ai-test` 连接测试端点（对齐 devpath-ai `app/api/ai-test/route.ts`）

BYOK onboarding 时用 session 配置发送测试消息，**区分上游鉴权失败（401）vs 本地错误（500）**——通过正则匹配 error.message 识别上游 401，避免用户误以为本地配置错误。

### 4.5 PWA 与离线（后台呼吸，对齐 devpath-ai `public/sw.js`）

#### 4.5.1 stale-while-revalidate 缓存策略

- **install**：预缓存静态资源（`/`、`/review`、`/stats`、`/manifest.json`）+ `skipWaiting()`。
- **activate**：清理旧缓存（`CACHE_NAME = "wordflow-v2"`）+ `clients.claim()`。
- **fetch**：非 GET 跳过；`/api/` 跳过（不缓存 API）；缓存命中 → 立即返回 + 后台更新缓存；缓存未命中 → fetch + 缓存响应 + 失败时 fallback 到首页。

#### 4.5.2 Web Push 推送通知

- `push` 事件：收到推送时弹"该复习了"提醒。
- `notificationclick` 事件：点击通知 → 聚焦/打开应用 + 导航到 `/review`。
- tag 去重（同一 tag 只保留最新通知）；图标 `/icons/icon-192.png`。

#### 4.5.3 Periodic Background Sync（P0.1 留存利器）

`periodicsync` 事件（tag: `"wordflow-background-check"`）→ `doBackgroundCheck()`：

1. SW 内直接用**原生 IndexedDB API**（`indexedDB.open("wordflow-db")`，object store `keyval`）——因 SW 无法 import idb-keyval/Dexie。
2. 查 `card:` 前缀卡片，统计 `dueCount`（`due <= now`）。
3. 查 `log:` 前缀记录，找最后学习时间。
4. 有待复习卡片且在线 → 推送"📚 今日有 N 词待复习"通知。
5. 连续 3 天未学习 → 推送"回来背词吧"通知（7 天以上换文案"很久没背词了，词书在等你"）。

**渐进增强**：periodicsync 需 Chrome 81+ / Edge 81+，不支持时降级为应用内打开时的提醒。

#### 4.5.4 离线策略

- 业务数据全 IndexedDB（离线完全可用：查词、复习、统计）。
- 静态资源 SW 缓存（离线可访问已缓存页面）。
- API 不缓存（离线时 AI 功能不可用但本地数据可读写）。

### 4.6 性能优化（对齐 devpath-ai `next.config.js`）

- **experimental.optimizePackageImports**：对 recharts / date-fns / @ai-sdk/openai / zod / nanoid / ts-fsrs / react-activity-calendar 做 per-module 拆分，优化 barrel export 库的 tree-shaking。
- **Webpack 分包**：
  - `recharts-vendor`：recharts + d3 全家桶（~500KB），只在统计页用，避免拖慢首屏。
  - `ai-sdk-vendor`：ai + @ai-sdk/openai，只在 AI 场景（例句/助记/批改）用。
  - `dict-vendor`：词典切片索引，按需 lazy load。
- **内存缓存**：`setItem` 自动失效对应内存缓存，避免脏读。
- **词典切片懒加载**：按前缀切片（`dict/{a-z}/{prefix}.json`，单片 <50KB），查词时按需 fetch + SW 缓存，首屏不加载全量词典。

### 4.7 安全头（对齐 devpath-ai `next.config.js` headers()）

`next.config.js` 的 `headers()` 配置四件套：

| 安全头 | 值 | 用途 |
|--------|---|------|
| HSTS | `max-age=63072000; includeSubDomains` | 强制 HTTPS |
| CSP | `default-src 'self'; script-src 'self' 'unsafe-inline'; ...` | 内容安全策略 |
| X-Frame-Options | `DENY` | 防点击劫持 |
| Permissions-Policy | `camera=(), microphone=(), geolocation=(), interest-cohort=(), browsing-topics=()` | 收敛浏览器能力 |

**注**：CSP nonce 模式因 `@cloudflare/next-on-pages` 限制暂回退（与 devpath-ai 一致），留待迁移 OpenNext adapter 后启用。守护测试 `__tests__/csp-nonce-guard.test.ts` + `__tests__/security-headers-guard.test.ts` 拦截回退（见第 8 章）。

### 4.8 成本测算（细化，免费模式可行性）

| 项目 | 免费层额度 | WordFlow 预估用量 | 备注 |
|------|-----------|------------------|------|
| Cloudflare Pages | 无限带宽，500 构建/日 | 静态资源全量托管 | 0 成本 |
| KV（4 namespace） | 每 namespace 100K 读/1K 写/日 | 手动同步模式约 1K DAU 可撑 | 三 KV 隔离避免配额争用 |
| Workers AI | 10K 推理/日 | 语义反查 + 释义嵌入 | bge-base-en-v1.5 |
| GLM Trial | — | 35 次/用户/日（例句+助记+批改） | 国内零梯子，降低首次体验摩擦 |
| DeepSeek Trial（备选） | — | 同上 | ¥0.003/用户/日 → 1K DAU 约 ¥90/月 |

- **三 KV 隔离配额**：AUTH_SESSIONS / AUTH_NONCES / AUTH_AUDIT 各自独立 1K 写/日，避免 session 高频写打爆业务 KV。
- **GLM Trial 优势**：国内零梯子可达，作为 Trial 默认 provider 降低用户首次体验摩擦（DeepSeek 需要梯子）。
- **结论：万级 DAU 内月成本 < ¥100，免费模式成立。**

---

## 5. 增长与运营

### 5.1 冷启动（0 → 1000 用户）

1. **开源仓库本身即获客渠道**：README 讲清"FSRS + 本地优先 + 真免费"，投 GitHub Trending / HelloGitHub。
2. 开发者社区首发：V2EX、即刻、少数派——打"程序员查文档生词自动复习"场景。
3. 内容营销：《为什么墨墨要买上限而 FSRS 是免费的》这类算法科普文，天然带争议性传播。

### 5.2 增长飞轮（1000 → 10 万）

- 学习报告分享海报（战绩卡 + 二维码）→ 备考社群自传播
- 浏览器划词插件（P1）→ 插件商店流量 → 反哺主站
- Anki 导入/导出兼容 → 承接 Anki 迁移用户
- **PWA periodicsync 作为留存利器**（对齐 devpath-ai "AI 从被调用变成在呼吸"模式）：用户授权后浏览器后台主动推送"该复习了"通知，无需用户打开应用即可召回，显著提升 WRU（每周复习用户数）——这是 WordFlow 与墨墨/扇贝等需用户主动打开 App 的竞品的核心差异化留存手段。

### 5.3 可持续性（不靠广告的活法）

- 成本控制：架构成本趋近 0（见 4.8）
- 可选收入（远期，不影响免费承诺）：BYOK 之外的 AI 增强包（重度 AI 用户订阅）、GitHub Sponsors、导出增值格式
- **Prompt 版本管理降低运维成本**（对齐 devpath-ai Prompt Registry）：版本指纹 + 快照测试让 AI prompt 迭代可追溯可回滚，降低 AI 功能长期维护风险——避免 prompt 漂移导致例句质量回归需紧急回滚。
- **红线：核心查词/背词/同步功能永久免费，写进 README 作为公开承诺。**

---

## 6. 里程碑与指标

### 6.1 路线图

| 阶段 | 周期 | 交付物 | 验证目标 |
|------|------|--------|---------|
| M1 MVP | 4 周 | 查词 + 收藏入队 + FSRS 复习 + 2 本词书 + Streak + PWA 离线 + quality-gate 接入 CI + **审计检查点：Round 1 功能正确性 + Round 2 安全性** | 核心闭环跑通，次日留存 >25% |
| M2 训练 | +3 周 | 拼写/听写模式 + 全词书 + 自定义词书 + 数据导出 + PWA periodicsync + Web Push + **审计检查点：Round 3 数据完整性 + Round 4 性能** | 7 日留存 >20% |
| M3 AI | +3 周 | AI 例句/助记 + 云同步 + 语义反查 + Prompt Registry + /api/ai-test + **审计检查点：Round 5 可维护性** | AI 功能使用率 >30% |
| M4 生态 | +4 周 | 浏览器划词插件 + 分享海报 + 造句批改 + AI Provider 多家切换 + **审计检查点：Round 6 用户体验** | 插件带来新增 >20% |
| M5 生态固化 | +4 周 | 六轮审计全跑通 + 性能基线建立 + 守护测试覆盖率 ≥80% + Prompt 版本管理全量上线 + AI Provider 多家切换完成 | 守护测试覆盖率 ≥80%，无 Critical/High 审计问题 |

### 6.2 北极星指标

**每周完成复习会话的用户数（WRU, Weekly Reviewing Users）** —— 同时反映留存和核心价值兑现。

辅助指标：查词→收藏转化率（目标 >15%）、收藏→次日复习完成率（目标 >50%）、D7 留存（目标 >20%）、平均 Streak 长度、PWA periodicsync 通知点击率（目标 >10%，M2 上线后追踪）。

### 6.3 风险与对策

| 风险 | 等级 | 对策 |
|------|------|------|
| 开源词典释义质量不如牛津/柯林斯 | 高 | AI 例句补强 + 词频排序释义 + 允许用户反馈修正 |
| FSRS 对新用户"冷启动不准" | 中 | 默认参数 + 词汇量测试预热（标熟词） |
| KV 免费额度被同步打爆 | 中 | 手动同步 + 增量 diff + 单用户频控 + 三 KV 隔离 |
| 用户习惯"图片记忆"觉得界面朴素 | 低 | 坚持定位，朴素即卖点（对标墨墨的成功） |
| AI 上游限流/封号 | 高 | Trial 多家 Provider 切换（GLM/DeepSeek/MiMo）+ BYOK 兜底 + `/api/ai-test` 主动探测 + 守护测试覆盖 |
| KV 配额打爆（session 高频写） | 中 | 三 KV 隔离（AUTH_SESSIONS/AUTH_NONCES/AUTH_AUDIT 独立配额）+ 手动同步 + 单用户频控 |
| Prompt 漂移导致 AI 质量回归 | 中 | 版本指纹（promptFingerprint）+ 快照测试强制 bump version + AICallRecord.promptVersion 可追溯 |

---

## 7. MVP 开发清单（M1 + M2 拓展，可直接开工）

### 7.1 M1 核心闭环（4 周）

```
Week 1  数据管线：compile-dict.ts（ECDICT→切片）+ compile-books.ts（考研/CET6）
        + zod schema 校验 + audit-dict.ts 实现 G1-G7 图谱校验规则
Week 2  查词：搜索框 + 前缀索引 + 词条页 + 收藏入队（Dexie 写卡）
        + Dexie dueAt 索引 + countDueCards 走索引（O(due) 而非 O(n)）
        + 动态 import("dexie") 避免 Edge runtime BroadcastChannel 泄漏
Week 3  复习：ts-fsrs 接入 + 卡片流 UI + 三档反馈 + 今日队列合并逻辑
        + ts-fsrs 三预设（conservative 0.95 / standard 0.9 / aggressive 0.8）
Week 4  留存壳：Streak + 热力图 + PWA（stale-while-revalidate 缓存词典切片）
        + 安全头四件套（HSTS/CSP/X-Frame-Options/Permissions-Policy）
        + quality-gate 脚本接入 CI（content:validate→freshness→lint→typecheck→test）
        + 里程碑审计 Round 1（功能正确性）+ Round 2（安全性）
```

### 7.2 M2 拓展（+3 周，留存增强）

```
Week 5  PWA periodicsync + Web Push：SW 内原生 IndexedDB API 查 due 卡片
        + 通知文案分级（3 天/7 天/14 天）+ 通知静默开关
Week 6  Prompt Registry + /api/ai-test：
        + 集中管理 AI system prompt（id/version/changelog）
        + promptFingerprint() 版本指纹 + 快照测试强制 bump version
        + Content Quality Charter 注入所有产答案 prompt
        + /api/ai-test BYOK 连接测试（区分上游 401 vs 本地 500）
```

技术栈锁定：Next.js 15 / React 19 / TypeScript / Tailwind / Dexie 4 / ts-fsrs 4.5 / Cloudflare Pages —— 与 devpath-ai 完全一致，脚手架和 CI（quality-gate → deploy）可直接拷贝。

---

## 8. 质量保障（对齐 devpath-ai 三层护栏 + 六轮审计）

### 8.1 三层质量护栏

| 层 | 触发时机 | 工具 | 内容 |
|----|---------|------|------|
| **预防层** | 写代码前 | `AGENTS.md` 编码规范 + UI 设计系统规范 | 统一组件库 `@/components/ui`（禁止原生 `<button>/<input>/<select>/<textarea>`）+ 设计令牌 + 暗色配对 |
| **检测层** | 每次 commit | `__tests__/*-guard.test.ts` 守护测试 | 拦截违规（原生表单元素、CSP nonce、安全头、prompt 版本漂移等） |
| **审计层** | 里程碑 / 定期 | 六轮深度审计方法论 | 系统性扫描技术债（见 8.3） |

**优先级**：`AGENTS.md` > `docs/DEVELOPMENT.md` > `docs/ARCHITECTURE.md` > 其他文档。

### 8.2 quality-gate 脚本（CI 强制 + husky pre-push）

```bash
npm run content:validate && npm run content:freshness && npm run lint && npm run typecheck && npm test
```

执行顺序：内容校验（zod + G1-G7）→ 来源新鲜度巡检 → ESLint（`--max-warnings 0`）→ TypeScript 类型检查（`strict: true`）→ Vitest 单元测试。

**husky pre-push 4 层门禁**：lint-staged 暂存文件 lint → typecheck → test → quality-gate 全量，杜绝部署失败连环事故。

**守护测试清单**（`__tests__/` 下）：

| 测试文件 | 拦截内容 |
|---------|---------|
| `no-native-form-elements.test.ts` | 扫描 `components/` 和 `app/` 下 `.tsx`（`components/ui/` 除外），发现原生表单元素即失败 |
| `ui-design-system-guard.test.ts` | UI 设计系统规范合规 |
| `csp-nonce-guard.test.ts` | CSP nonce 模式回退拦截 |
| `security-headers-guard.test.ts` | 安全头四件套配置完整性 |
| `prompts.test.ts` | `PROMPT_VERSION_HASHES` 快照，强制 prompt 改内容必 bump version |

### 8.3 六轮审计方法论（适配词典学习域）

**核心原则**：按优先级逐轮扫描，每轮"扫描→清单→确认→修复→验证→下一轮"，不得跳轮。

| Round | 维度 | 优先级 | WordFlow 重点检查点 |
|-------|------|--------|---------------------|
| 1 | 功能正确性 | Critical | 空指针解引用 / Promise 链无 .catch / 边界条件（空词书/空搜索/0 词频）/ `as any` 绕过类型 / API 请求体未校验 |
| 2 | 安全性 | High | apiKey 明文存储 / nonce 重放 / HMAC 时序攻击 / CSP 绕过 / XSS（用户自定义词书输入） |
| 3 | 数据完整性 | High | FSRS 卡片状态机非法转换 / tombstone 清理逻辑 / LWW 合并冲突 / 词书去重 / V1-V4 降级规则 |
| 4 | 性能 | Medium | dueAt 索引命中率 / 分包加载（recharts-vendor/ai-sdk-vendor）/ 词典切片懒加载 / SW 缓存命中率 |
| 5 | 可维护性 | Medium | prompt 版本指纹一致性 / 类型覆盖率 / 守护测试覆盖率 / 文档与代码同步 |
| 6 | 用户体验 | Low | 三秒查词收藏 / 键盘快捷键（1/2/3/空格）/ 中断友好（进度实时落库）/ 通知文案分级 |

**触发时机**：每个里程碑（M1-M5）结束前必跑对应 Round（见 6.1 路线图）；定期（每季度）全量六轮。

### 8.4 CI/CD 流水线

```
Job 1: quality-gate（lint + typecheck + test + content:validate + content:freshness）
  ↓ needs: quality-gate
Job 2: deploy
  ├── npm ci（Node 22）
  ├── npm run build
  ├── npx @cloudflare/next-on-pages（构建为 Cloudflare Pages 兼容格式）
  └── wrangler pages deploy .vercel/output/static --project-name=wordflow-ai
```

**触发条件**：push 到 main/develop，路径过滤（`app/components/lib/public/__tests__/scripts/` 等变更才触发）。

**测试矩阵**：
- 单元测试：Vitest 1.6 + Testing Library + jsdom
- E2E 测试：Playwright（`npm run test:e2e`）
- 性能测试：`npm run test:perf`（`vitest run --testNamePattern='benchmark|perf'`）
- 覆盖率：`npm run test:coverage`（M5 目标 ≥80%）

---
